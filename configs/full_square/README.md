# Configs - full_square

Configuracoes especificas da linha `full_square`.

Arquivos canonicos atuais:

- `protocol_default.json`
- `protocol_default.yaml`
- `all_regimes_sel4curr.json`
- `all_regimes_full_dataset.json`
- `regimes_0p8m_only.json`

Os caminhos legados em `configs/*.json` e `configs/*.yaml` permanecem
disponiveis por compatibilidade, apontando para estes arquivos.
