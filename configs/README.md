# Configs Map

The canonical configuration layout is split into:

- `common/`
- `full_square/`
- `full_circle/`
- `benchmarks/modulations/`

## Compatibility note

Some legacy root-level config paths are preserved as symlinks for backward
compatibility. The canonical scientific home for the current protocol JSON/YAML
files is `configs/full_square/`.
